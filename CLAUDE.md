@AGENTS.md

## Claude Code 专属补充

- 本地记忆位于 `~/.claude/projects/C--aiWorkspace-clowder-ai/memory/`，索引为同目录的 `MEMORY.md`。仅作辅助召回，不替代 AGENTS.md 中的固定规则。
- `.claude/settings.json` 配置了 SessionStart / PreCompact / Stop hooks，自动提醒记忆维护。
- 当前身份：`Clowder Claude <claude@clowder.local>`（除非任务启动上下文覆盖）。
