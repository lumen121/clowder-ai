# T5 工作项状态机复盘

> 状态：当前基线
> 所属：复盘
> 规则效力：改进输入，不自动成为当前规则
> 维护角色：Claude
> 日期：2026-06-20

## 做得好的

- **两层 API 设计** — `transition()` 纯验证 + `transitionWorkItem()` 读-验-写一体，Codex 复核认可。调用方按需选层，后续模块不会手写字段拼装。
- **Codex Review 三轮闭环** — 初审发现 6 项 P1/P2，修正后复核 3 条补齐，最终通过。Review 过程对"纯函数到可复用接口"的架构推升有实质贡献。
- **`metadata.blocking` 结构** — 从下划线内部字段改为命名空间化对象，页面可直接解释阻塞原因/来源/时间。

## 需要改进的

1. **worktree 隔离拖延** — 初版在既有分支直接开发，Codex 初审指出后才迁到 `claude/t5-state-machine`。工具完全支持（EnterWorktree），没有不隔离的理由。
2. **`settings.json` 混入** — `worktree.baseRef` 配置不属于 T5，被 Codex 在复核中指出。配置变更应在独立分支/提交中处理。
3. **复盘滞后** — T4 review 时被要求"按启动包输出"，T5 又漏了复盘。应该在交付时就走完整闭环，不等外部提醒。
