# T3 / T2 Review 轻量复盘

> 状态：当前基线
> 所属：复盘
> 规则效力：改进输入，不自动成为当前规则
> 维护角色：Claude
> 日期：2026-06-19

## 做得好的

- **T3 架构简洁** — 单文件每模型、零依赖、工厂+Store 分层，符合"不过早设计成复杂数据库"约束。三轮 Review 发现的引用泄露修复模式已被 Codex 确认有效。
- **T2 Review 精准** — 3 条建议全被采纳（`type_label` 移除、Windows 大小写防护、CLI 显式分发），无争议。

## 需要改进的

1. **提交边界不清** — 把 Codex 产出一起提交了（`git add -A`）。应先确认文件归属再动手，避免替其他 Agent 提交。
2. **证据链补录反复** — A1/A2/A3 来回多轮：先缺 Review 链接 → 再缺逐项确认表 → 再缺锚点。模板要求"输出记录必须包含可追踪证据"，下次启动包直接写全锚点，省掉补录。
3. **产品裁决预判不准** — P0-14/15/16 变更时建议"页面录入推迟到 T13"，产品直接否决。教训：已批准的 P0 基线，执行层不能建议推迟，只能建议如何落地。
